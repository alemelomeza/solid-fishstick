<?php

describe('InvoiceService', function() {
    describe('->generateInvoices()', function() {
        it('should query the respository for uninvoiced Ordres');
        it('should return an Invoice for each uninvoiced Order');
    });
});